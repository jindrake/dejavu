- args:
    sql: CREATE TABLE "public"."user_activity"("id" uuid NOT NULL, "activity_type"
      text NOT NULL, "user_id" uuid NOT NULL, "note" text , "topic_id" uuid NOT NULL,
      "question_id" uuid NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("user_id") REFERENCES
      "public"."user"("id") ON UPDATE restrict ON DELETE restrict, FOREIGN KEY ("topic_id")
      REFERENCES "public"."topic"("id") ON UPDATE restrict ON DELETE restrict, FOREIGN
      KEY ("question_id") REFERENCES "public"."question"("id") ON UPDATE restrict
      ON DELETE restrict, UNIQUE ("id"));
  type: run_sql
- args:
    name: user_activity
    schema: public
  type: add_existing_table_or_view
