- args:
    sql: CREATE TABLE "public"."question"("id" uuid NOT NULL, "question" text NOT
      NULL, "type" text NOT NULL, "creator_id" uuid NOT NULL, "answer" text NOT NULL,
      PRIMARY KEY ("id") , FOREIGN KEY ("creator_id") REFERENCES "public"."user"("id")
      ON UPDATE restrict ON DELETE restrict, UNIQUE ("id"));
  type: run_sql
- args:
    name: question
    schema: public
  type: add_existing_table_or_view
